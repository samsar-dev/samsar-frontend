/**
 * Minimal CSS utilities for mobile optimization
 */

/**
 * Optimize CSS delivery for mobile devices - simplified
 */
export function optimizeCSSForMobile(): void {
  // No-op - CSS optimization is handled by build tools
  // This function is kept for backward compatibility
}

/**
 * Placeholder for backward compatibility
 */
export function preloadCriticalFonts(): void {
  // No-op - fonts handled by browser
}

/**
 * Placeholder for backward compatibility  
 */
export function loadCSSWithPriority(): Promise<void> {
  return Promise.resolve();
}
